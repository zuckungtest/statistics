name: test-pat
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Show token env
        run: |
          echo "Secret PAT length: ${#PAT}"
          python3 - <<'EOF'
          import os
          t = os.getenv("PAT")
          print("TOKEN vorhanden:", bool(t))
          print("Länge:", len(t) if t else 0)
          EOF
        env:
          MY_PAT: ${{ secrets.PAT }}
